import { callCrossmintAPI } from "@/app/utils/crossmint";
import { NextRequest, NextResponse } from "next/server";

// export async function GET(req: NextRequest, { params }: { params: { wallet: string } }) {
//   const apiPath = `v1-alpha1/wallets/polygon:${params.wallet}/nfts`;
//   const apiResponse = await callCrossmintAPI(apiPath, { method: "GET" });
//   return NextResponse.json(apiResponse);
// }
